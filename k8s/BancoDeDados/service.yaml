apiVersion: v1
kind: Service
metadata:
  name: sqlserver-service
spec:
  type: NodePort
  selector:
    app: sqlserver-fiap
  ports:
    - name: sql
      protocol: TCP
      port: 1433
      targetPort: 1433
      nodePort: 32000 