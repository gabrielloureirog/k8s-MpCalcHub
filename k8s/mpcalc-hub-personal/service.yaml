apiVersion: v1
kind: Service
metadata:
  name: mpcalchub-personal-service
spec:
  type: NodePort
  selector:
    app: mpcalchub-personal
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5056
      nodePort: 32001