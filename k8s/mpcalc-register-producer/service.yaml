apiVersion: v1
kind: Service
metadata:
  name: mpcalchub-register-producer-service
spec:
  type: NodePort
  selector:
    app: mpcalchub-register-producer
  ports:
    - port: 80
      targetPort: 5022
      nodePort: 32002
        