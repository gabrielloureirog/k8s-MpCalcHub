apiVersion: v1
kind: Service
metadata:
  name: mpcalchub-deleter-producer-service
spec:
  type: NodePort
  selector:
    app: mpcalchub-deleter-producer
  ports:
    - port: 80
      targetPort: 5024
      nodePort: 32003
